/**
  * Sample config file, replace with own values
  * the config.json file should contain the same key-value pairs if no in Dev mode.
  */

window.config = {
	"org_url": "orgs/github",
	"user": "example",
	"token": "[TOKEN]"
}
